<h2><%= @survey.title %></h2>
<%= link_to "Add Question To Survey", new_survey_question_path(@survey) %>

<% if @survey.questions.any? %>
  <p>We got a lotta questions:</p>
  <ul>
    <% @survey.questions.each do |q| %>
      <li><%= q.prompt %></li>
    <% end %>
  </ul>
<% end %>
